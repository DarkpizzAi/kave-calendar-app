# Never commit data or secrets to this public repo.
data/
*.local
.env

# Local preview snapshots generated from kave-hub, if Compass grows them the
# way Spoon did. Dev only, never for the public repo.
*.dev.json

# Local tooling: the preview launch config, kept out of the public repo.
# It must stay on disk here - the preview tools read it relative to this
# folder and it runs dev_server.py from it - so ignore, never move.
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
